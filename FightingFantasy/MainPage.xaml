<Page
    x:Class="FightingFantasy.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FightingFantasy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:panels="using:FightingFantasy.Panels"
    xmlns:viewmodels="using:FightingFantasy.ViewModels"
    xmlns:converters="using:FightingFantasy.Converters">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Page.DataContext>
        <viewmodels:MainPageViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <Grid>
        <panels:StartScreen HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Path=IsStartScreenVisible, Converter={StaticResource BooleanToVisibilityConverter}, Mode=TwoWay}"/>
        <panels:NewCharacter Visibility="{Binding Path=IsNewCharacterScreenVisible, Converter={StaticResource BooleanToVisibilityConverter}, Mode=TwoWay}"/>
    </Grid>
</Page>